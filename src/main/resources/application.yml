server:
  port: ${PORT:8080}

spring:
  application:
    name: automated-incident-response
  datasource:
    url: ${DATABASE_URL:postgresql://incidentdb_user:0IMi4ANZ3FYG5Wlq30QhtfDlGv9RVYuJ@dpg-d3lofradbo4c73b99gl0-a/incidentdb}
    username: ${DB_USERNAME:incidentdb_user}
    password: ${DB_PASSWORD:0IMi4ANZ3FYG5Wlq30QhtfDlGv9RVYuJ}
    driver-class-name: org.postgresql.Driver
  mail:
    host: imap.gmail.com
    port: 993
    username: vbvubuvbv@gmail.com
    password: xizr cbgy mrrf lvnr
    properties:
      mail:
        imap:
          ssl:
            enable: true
            trust: imap.gmail.com
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    open-in-view: false

logging:
  level:
    root: INFO
    com.team.incidentresponse: DEBUG

security:
  user:
    name: ${SPRING_SECURITY_USER_NAME:admin}
    password: ${SPRING_SECURITY_USER_PASSWORD:admin123}

websocket:
  endpoint: /incident-feed
  topic: /topic/incidents

scheduler:
  enabled: true
  incident-check-interval: 30000

playbook:
  path: classpath:playbooks/

# Twilio SMS Configuration (optional)
twilio:
  account:
    sid: ${TWILIO_ACCOUNT_SID:}
  auth:
    token: ${TWILIO_AUTH_TOKEN:}
  phone:
    number: ${TWILIO_PHONE_NUMBER:}

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
