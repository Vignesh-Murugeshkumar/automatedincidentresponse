# Lightweight Dockerfile for running the already-built JAR in target/
# This avoids running Maven inside the container and uses the existing artifact.

FROM openjdk:21-jdk-slim
WORKDIR /app

# Copy the built, repackaged Spring Boot JAR (executable)
# The repackaged artifact is `automated-incident-response-1.0.0.jar` (the original is renamed to .jar.original)
COPY target/automated-incident-response-1.0.0.jar app.jar

EXPOSE 8080
ENTRYPOINT ["java","-jar","app.jar"]
